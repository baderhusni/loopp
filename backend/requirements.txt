# Runtime
fastapi>=0.115
uvicorn[standard]>=0.30
# Real agent engine (subscription-auth Claude Agent SDK). The app also ships a
# deterministic mock engine, so it still runs if this is unavailable.
claude-agent-sdk>=0.2.0

# Dev / tests
pytest>=8.0
httpx>=0.27
